steps:
  # Copiar requirements.txt del nivel superior al directorio gcp/ingestor_cloud
  - name: 'bash'
    args: ['-c', 'cp requirements.txt gcp/ingestor_cloud/requirements.txt']
  
  # Copiar utils.py desde dags/utils al directorio gcp/ingestor_cloud
  - name: 'bash'
    args: ['-c', 'cp dags/utils/utils.py gcp/ingestor_cloud/utils.py']
  
  - name: 'gcr.io/cloud-builders/docker'
    dir: 'gcp/ingestor_cloud'
    args: ['build', '-t', 'europe-southwest1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/insert-ducklake:latest', '.']
  
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'europe-southwest1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/insert-ducklake:latest']

# Imágenes a crear
images:
  - 'europe-southwest1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/insert-ducklake:latest'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
